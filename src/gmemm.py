def gmemm_only_word_observed_ll(word, parameter):
    """Use dynamic programming(specifically Variable Elimination)
    trick to marginalize over the unobserved tags"""
    
    
    return s

def gmemm_full_observed_ll(word, tag, parameter):
    """Return the loglikelihood under the GMEMM model of a sequence of
    word, tag pairs. This is the simplest case for computation"""
    s=0.0
    
    return s


